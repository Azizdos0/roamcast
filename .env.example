# Open-Meteo's public endpoints are used when these values are omitted.
# Set the customer endpoints and key before a commercial launch.
WEATHER_API_BASE_URL=https://api.open-meteo.com/v1/forecast
GEOCODING_API_BASE_URL=https://geocoding-api.open-meteo.com/v1/search
WEATHER_API_KEY=
